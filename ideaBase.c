#include <stdio.h>

int main() {
    //Creamos variables globales(que seguramente se van a rescribir en el transcurso del programa
    int soluconesMATRICES[10];
    char respuesta[2] = "si";
    char seguirJugando[2] = "si";
    int cantidadPartidas = 0;
    int apuesta = 0;
    int resolvido = 0;
    int historialPuntos[] = {0};

    //Mientras el usuario responda que SI quiere seguir jugando otra partida y
    //NO SE LEGUE a la cantidad maxima de partidas, seguiremos jugando OTRA partida
    while(respuesta == "si" && cantidadPartidas != 10) {
        //Reinicializacion de variables que usamos dentro de la partida
        apuesta = 0;

        //Prguntamos apuesta

        //ACA se va a hacer la eleccion de una matriz no repetida

        while(seguirJugando == "si" && resolvido == 0){

        }

        //Guardo los puntos en el array de historial
        //Pregunto si quiere jugar otra partida
    }   

//asdwefjierjm´v

    return 0;
}